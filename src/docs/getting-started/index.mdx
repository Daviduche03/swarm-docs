# Getting started

A quickstart for using the API.

## Authentication

Describe how to authenticate: API keys, bearer tokens, or OAuth.

## Your first request

```ts
// Replace with a real example for the API you are documenting
const response = await fetch("https://api.example.com/v1/ping", {
  headers: { Authorization: "Bearer YOUR_API_KEY" },
});
```

> **Generated by Weldrr.** Replace this placeholder with a real quickstart.
