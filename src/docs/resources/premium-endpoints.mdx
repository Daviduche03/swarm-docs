# Premium endpoints

Access to these endpoints is restricted to Pro and Ultra plans. To upgrade, see [Pricing](/docs/resources/pricing).

## Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/v1/graph-workflow/completions` | POST | Execute graph workflows with directed agent nodes and edges — complex multi-agent collaboration with parallel execution and automatic compilation |
| `/v1/agent/batch/completions` | POST | Process multiple agent tasks in parallel batches with high-throughput batch processing |
| `/v1/swarm/batch/completions` | POST | Execute batch swarm completions with concurrent multi-agent execution at scale |
| `/v1/reasoning-agent/completions` | POST | Execute advanced reasoning agents using architectures like self-consistency, majority voting, and iterative refinement |
| `/v1/batched-grid-workflow/completions` | POST | Execute multiple tasks across multiple agents in a grid pattern — creates task-agent matrices for comparative analysis |

## Why these endpoints require premium access

These endpoints are resource-intensive operations that provide:

- **High-throughput batch processing** — process hundreds of tasks concurrently
- **Concurrent multi-agent execution** — run multiple specialized agents simultaneously
- **Advanced reasoning capabilities** — sophisticated reasoning architectures for complex problem-solving
- **Enterprise-scale workflow orchestration** — coordinate large-scale, multi-step agent workflows

Premium tier restrictions ensure sustainable infrastructure costs while
maintaining quality service for enterprise customers.

## Error response for free tier users

Free tier users attempting to access these endpoints receive a **403 Forbidden**
response with upgrade instructions:

```json
{
  "error": {
    "type": "premium_access_required",
    "message": "Premium subscription required",
    "detail": "Access to Batch Agent Completions requires a premium subscription. ... To unlock this and other premium capabilities, please upgrade your account at https://swarms.world/platform/account.",
    "status_code": 403,
    "upgrade_url": "https://swarms.world/platform/account",
    "documentation": "https://docs.swarms.ai"
  }
}
```

The `detail` message is tailored per endpoint (naming the specific feature you tried to access).

## Notes

- These endpoints may have different rate limits aligned with premium tiers.
- Ensure your API key is associated with a Pro, Ultra, or Premium subscription.
- Premium endpoints are billable: your account's total credit balance must be above **$1.00**, or the request is rejected with a **402 Payment Required** error before any work runs. Check your balance with `GET /v1/account/credits`.
- Certain models (e.g. GPT-5, Claude 4 Opus/Sonnet, Gemini 2.5 Pro) are also gated to premium subscribers on regular endpoints — see [Pricing](/docs/resources/pricing).

## See also

- [Batch agent completions](/docs/reference/agent-batch-completions)
- [Batch swarm completions](/docs/reference/swarm-batch-completions)
- [Reasoning agent completions](/docs/reference/reasoning-agent-completions)
- [Graph workflow completions](/docs/reference/graph-workflow-completions)
- [Batched grid workflow completions](/docs/reference/batched-grid-workflow-completions)
